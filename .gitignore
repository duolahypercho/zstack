# per-run working material: build outputs you did not choose to publish
runs/
__pycache__/
*.pyc
*.blend1
.DS_Store
# reference photos of real products are private working material, never published
refs/
*.local.*
# build intermediates (regenerated by every build)
*.blend1
examples/*/checks/*.npz
examples/*/checks/curves.round*.json
examples/*/checks/cameras.round*.json
